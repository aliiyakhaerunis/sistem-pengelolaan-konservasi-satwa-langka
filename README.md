# GraphQL PHP MySQL CRUD Project

### 📂 Struktur Project
- index.php : Entry GraphQL endpoint
- config/database.php : Koneksi PDO
- schema/ : Semua type, query, mutation GraphQL
- db.sql : SQL untuk database
- .env : Setting database + Basic Auth

---

### 🛠 Cara Setup

1. Extract semua file project.
2. Jalankan:
   ```bash
   composer install